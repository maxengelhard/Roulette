import React from 'react'


const BlackRed = (props) => (
    <div className={props.className} style={props.style}>
        <span className={props.span}><p>{props.number}</p></span>
    </div>
    )

export default BlackRed
